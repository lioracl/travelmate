(function(){
  var form=document.querySelector('[data-destination-form]'),list=document.querySelector('[data-trip-list]');
  function daysBetween(start,end){return Math.floor((new Date(end)-new Date(start))/86400000)+1}
  function formatDate(value){return new Intl.DateTimeFormat('he-IL',{day:'numeric',month:'long',year:'numeric'}).format(new Date(value+'T12:00:00'))}
  function tripCard(trip){var link=document.createElement('a');link.className='trip-card';link.href='trip/custom/index.html?id='+encodeURIComponent(trip.id);link.style.backgroundImage="url('https://images.unsplash.com/photo-1488646953014-85cb44e25828?auto=format&fit=crop&w=1200&q=80')";link.innerHTML='<span class="trip-overlay"></span><span class="trip-flag">✈️</span><span class="trip-copy"><h2>'+escapeText(trip.country)+'</h2><p>'+escapeText(trip.city)+'</p><span class="tag">'+formatDate(trip.start)+' – '+formatDate(trip.end)+'</span> <span class="tag">'+trip.days+' ימים</span><strong>פתיחת הטיול <i class="fa-solid fa-arrow-left"></i></strong></span>';return link}
  function escapeText(value){return String(value).replace(/[&<>"']/g,function(char){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]})}
  function getTrips(){try{return JSON.parse(localStorage.getItem('travelmate-trips')||'[]')}catch(error){return[]}}
  getTrips().forEach(function(trip){list.insertBefore(tripCard(trip),list.querySelector('.add-destination'))});
  if(!form)return;var today=new Date(),next=new Date(today.getTime()+7*86400000);form.elements.start.value=today.toISOString().slice(0,10);form.elements.end.value=next.toISOString().slice(0,10);
  form.addEventListener('submit',function(event){event.preventDefault();var error=form.querySelector('[data-form-error]'),days=daysBetween(form.elements.start.value,form.elements.end.value);if(days<1){error.textContent='תאריך החזרה חייב להיות אחרי תאריך היציאה.';return}if(days>60){error.textContent='אפשר לבנות כרגע טיול של עד 60 ימים.';return}var trip={id:String(Date.now()),country:form.elements.country.value.trim(),city:form.elements.city.value.trim(),start:form.elements.start.value,end:form.elements.end.value,budget:Number(form.elements.budget.value||2500),type:form.elements.type.value,days:days};var trips=getTrips();trips.push(trip);localStorage.setItem('travelmate-trips',JSON.stringify(trips));location.href='trip/custom/index.html?id='+encodeURIComponent(trip.id)})
})();
